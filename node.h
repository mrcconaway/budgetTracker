#pragma once

#include <cstdlib>
#include "date.h"
#include "month.h"
#include "budget.h"

class node
{
public:
    // node(node* nInit = nullptr, node* pInit = nullptr);
    

private:
    // BUDGET budget;
private:
    node* next;
    node* prev;
};